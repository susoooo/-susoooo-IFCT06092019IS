1-Escriba un programa que pida dos números enteros y escriba qué números son pares y cuáles impares desde el primero hasta el segundo

a=input()
b=input()
c=int(a)
d=int(b) 
for i in range(c,d):
	if (i % 2 == 0):
		print(i," es par")
	else:
		print(i," es impar")
		

2-Escriba un programa que pida un número entero mayor que cero y que escriba sus divisores.

a=input()
b=int(a)
if (b>0):
	for i in range(1,b):
		if (b % i == 0):
			i
else:
	print("Introduzca un número mayor de 0")


3-Escriba un programa que pregunte cuántos números se van a introducir, pida esos números, y muestre un mensaje cada vez que un número no sea mayor que el primero

print("¿Cuántos números va a introducir?")
numeros=input()
n=int(numeros)
for i in range(0,n):
	if (i==0):
		a=input()
		b=int(a)
		mem=b
	else:
		a=input()
		b=int(a)
		if (b<mem):
			print(b," no es mayor que el primero (",mem,")")


4-Escriba un programa que pregunte cuántos números se van a introducir, pida esos números y escriba cuántos negativos ha introducido.

print("¿Cuántos números va a introducir?")
numeros=input()
n=int(numeros)
conta=0
for i in range(0,n):
	a=input()
	b=int(a)
	if (b<0):
		conta += 1
print("Ha introducido ",conta," números negativos")


5-Escriba un programa que pregunte cuántos números se van a introducir, pida esos números, y diga al final cuántos han sido pares y cuántos impares.

print("¿Cuántos números va a introducir?")
numeros=input()
n=int(numeros)
contap=0
contai=0
for i in range(0,n):
	a=input()
	b=int(a)
	if (b % 2 == 0):
		contap += 1
	else:
		contai += 1
print("Ha introducido ",contap," números pares y ",contai," números impares")


6-Escriba un programa que pregunte cuantos números se van a introducir, pida esos números y calcule su suma.

print("¿Cuántos números va a introducir?")
numeros=input()
n=int(numeros)
suma=0
for i in range(0,n):
	a=input()
	b=int(a)
	suma=suma+b
print("Suma=",suma)











