1-Escriba un programa que pida dos números enteros. El programa pedirá de nuevo el segundo número mientras no sea mayor que el primero. El programa terminará escribiendo los dos números.

a=input()
c=int(a)
d=0
while d <= c:
	b=input()
	d=int(b)
print("Primer número: ",c," Segundo número: ",d)


2-Escriba un programa que pida números decimales mientras el usuario escriba número mayores que el primero.

a=input()
c=float(a)
d=c+1
while d > c:
	b=input()
	d=float(b)


3-Escriba un programa que pida la cantidad de números positivos que se tienen que escribir y a continuación pida números hasta que se haya escrito la cantidad de números positivos indicada.

print("¿Cuántos números va a introducir?")
numeros=input()
n=int(numeros)
i=0
while i < n:
	a=input()
	c=int(a)
	if(c>0):
		i+=1


4-Escriba un programa que pida primero dos números enteros (mínimo y máximo) y que después pida números enteros situados entre ellos. El programa terminará cuando se escriba un número que no esté comprendido entre los dos valores iniciales. El programa termina escribiendo la cantidad de números escritos.

a=input()
b=input()
c=int(a)
d=int(b)
i=c+1 
cont=0
while i > c and i < d:
	x=input()
	i=int(x)
	cont+=1
print("Se han escrito ",cont," números")


5-Escriba un programa que pida números pares mientras el usuario indique que quiere seguir introduciendo números. Para indicar que quiere seguir escribiendo números, el usuario deberá contestar S o s a la pregunta.

opcion="S"
while opcion == "S" or opcion == "s":
	a=input()
	c=int(a)
	if (c % 2 == 0):
		print("¿Quiere seguir introduciendo números?")
		opcion=input()
	else:
		print("El número no es par")















